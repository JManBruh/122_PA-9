#include "tilemap.hpp"

//runs what used to be in main
void game();
//literally just for-loops making the tilemap positions!
vector<Vector2f> mapGen(vector<Vector2f> array, float scaleFactor, int tileSize);